# This root render.yaml should NOT be used for deployment
# Use individual render.yaml files in backend/ and ai-models/ directories
# Deploy each service separately on Render dashboard

# For backend: Use backend/render.yaml
# For AI models: Use ai-models/render.yaml